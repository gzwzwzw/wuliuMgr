server:
  port: 9461
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/logistics_manager?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8
    username: root
    password: '123456'
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true
logging:
  level:
    root: info
